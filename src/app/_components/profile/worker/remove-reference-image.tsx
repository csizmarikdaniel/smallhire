import RemoveIcon from "../../icons/remove";

const RemoveReferenceImage = () => {
  return (
    <div className="absolute -right-3 -top-3 z-10 rounded-full bg-white p-1">
      <RemoveIcon height={20} width={20} />
    </div>
  );
};

export default RemoveReferenceImage;
